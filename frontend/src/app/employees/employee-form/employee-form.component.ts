import { Component } from '@angular/core';

// Placeholder — fleshed out in the next phase (Reactive Forms, validation,
// submit-guard, error handling). Exists now so routing resolves for Phase 5.
@Component({
  selector: 'app-employee-form',
  standalone: true,
  template: `<p>Employee form coming in the next phase.</p>`,
})
export class EmployeeFormComponent {}
